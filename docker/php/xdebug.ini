; Xdebug configuration for coverage runs
; Note: extension is enabled via docker-php-ext-enable (do not set zend_extension here)
; Keep Xdebug inactive by default; enable it explicitly for coverage runs
xdebug.mode=off
xdebug.start_with_request=trigger
; Reduce noise in logs
xdebug.log_level=0

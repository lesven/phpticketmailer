{% extends 'base.html.twig' %}

{% block title %}Unbekannte Benutzer - {{ parent() }}{% endblock %}

{% block body %}
    <h1 class="mb-4">Unbekannte Benutzer</h1>
    
    <div class="alert alert-info">
        <p>Für folgende in der CSV-Datei gefundene Benutzer sind keine E-Mail-Adressen hinterlegt. 
        Bitte geben Sie die E-Mail-Adressen ein, damit die Zufriedenheits-E-Mails versendet werden können.</p>
    </div>
    
    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0">E-Mail-Adressen zuweisen</h5>
        </div>
        <div class="card-body">
            <form method="post">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Benutzername</th>
                                <th>E-Mail-Adresse</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for username in unknownUsers %}
                                <tr>
                                    <td>{{ username }}</td>
                                    <td>
                                        <input type="email" name="email_{{ username }}" class="form-control" required>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                
                <div class="d-grid gap-2 mt-3">
                    <button type="submit" class="btn btn-primary">E-Mail-Adressen speichern und fortfahren</button>
                </div>
            </form>
        </div>
    </div>
    
    <div class="mt-3">
        <a href="{{ path('csv_upload') }}" class="btn btn-secondary">Zurück zum CSV-Upload</a>
    </div>
{% endblock %}